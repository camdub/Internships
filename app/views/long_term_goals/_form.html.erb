<%= form_for(@long_term_goal) do |f| %>
  <% if @long_term_goal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@long_term_goal.errors.count, "error") %> prohibited this long_term_goal from being saved:</h2>

      <ul>
      <% @long_term_goal.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
